cmake_minimum_required(VERSION 3.12) # top level cmake must have this
project(smorgasbord) # top level cmake should have a project specification
include(smorgasbord.cmake) # body as include to make it easier to find among CmakeLists.txt files in an IDE
